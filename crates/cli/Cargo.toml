[package]
name = "plinth-cli"
version.workspace = true
edition.workspace = true

[[bin]]
name = "plinth"
path = "src/main.rs"

[dependencies]
# Workspace dependencies
plinth-shared = { workspace = true, features = ["config-toml"] }
toml = { workspace = true }

# CLI
clap = { workspace = true, features = ["derive", "env"] }
clap_complete = { workspace = true }
clap_complete_nushell = { workspace = true }
anyhow = { workspace = true }
console = { workspace = true }
indicatif = { workspace = true }
inquire = { workspace = true }

# HTTP client
reqwest = { workspace = true, features = ["json", "multipart", "stream"] }
tokio = { workspace = true, features = ["full"] }

# Content processing
pulldown-cmark = { workspace = true }
gray_matter = { workspace = true }

# Typst compilation
typst-as-lib = { workspace = true, features = ["typst-html", "typst-kit-embed-fonts"] }
typst-html = { workspace = true }

# Vector embeddings
fastembed = { workspace = true }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml = { workspace = true }

# Image processing / upload
regex = { workspace = true }
mime_guess = { workspace = true }
sha2 = { workspace = true }
chrono = { workspace = true }

[features]
default = ["brick-blog", "brick-todo"]
brick-blog = ["plinth-shared/brick-blog"]
brick-todo = ["plinth-shared/brick-todo"]
